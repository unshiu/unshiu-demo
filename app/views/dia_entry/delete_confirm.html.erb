<% content_for(:header_title) do %>
<%= t('view.noun.dia_diary') %>削除
<% end %>

<div class="error">
	<%= t('activerecord.attributes.dia_entry.title') %>
	「<%= @entry.title %>」
	の<%= t('view.noun.dia_diary') %>を削除します。
	この<%= t('view.noun.dia_diary') %>に<%= t('view.noun.dia_entry_comment') %>が存在した場合、<%= t('view.noun.dia_entry_comment') %>も同時に削除されます。
	よろしいですか？
</div>

<% form_tag :action => @delete_action_name, :id => @entry.id do %>
	<%= hidden_field_tag 'cancel_to', params[:cancel_to] %>
	<%= submit_button_tag("削除する") %>
	<%= cancel_button_tag %>
<% end %>